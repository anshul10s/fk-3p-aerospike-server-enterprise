#!/bin/bash -e

AS_SERVER_VERSION=3.9.1.1
PAC_NAME=fk-3p-aerospike-server-enterprise-${AS_SERVER_VERSION}
BUILD_VER=$(date +%Y%m%d-%H%M)
BASE_DIR=package-common

./make-fk-3p-aerospike-deb $BASE_DIR $PAC_NAME $BUILD_VER $AS_SERVER_VERSION

#Do customization here if rquired to the deb folder 

./create-deb deb ${PAC_NAME}.deb

rm -rf deb
